import React from 'react';

const NotFoundPage: React.FC<any> = (props: React.PropsWithChildren<any>) => {  
    return (
      <div>
        NotFound
      </div>
    );
  };
  
  export default NotFoundPage;